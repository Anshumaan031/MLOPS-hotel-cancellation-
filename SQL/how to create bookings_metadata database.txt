Run the following command lines in ubuntu terminal:

mysql -u root -p 
<ENTER YOUR ROOT PASSWORD> 
create schema hotel_datawarehouse;
mysql -u root -p hotel_datawarehouse < /home/sid685/sql/bookings_metadata.sql
quit